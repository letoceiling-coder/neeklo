Задача: доработать страницу редактирования продукта `/admin/decisions/products/:id/edit` в админке CMS.

Цель: добавить недостающие управляющие поля, аналогично разделу "Услуги", для более гибкой настройки карточек продуктов через CMS.

1. Найди текущую модель, используемую для "Products" и аккуратно расширь её следующими полями:
  - `short_description` — тип `Text`, textarea с подсказкой: "Короткое описание для карточек и первого экрана (140–220 символов)", не обязательное.
  - `card_preview_image` — отдельное поле для загрузки изображения на првеью проверить. Используется **только в карточках листинга**. Не влияет на основное изображение.
  - `page_title` — строка, заголовок страницы (H1).
  - `page_subtitle` — строка, подзаголовок/лид страницы.
  - `cta_text` — строка, текст кнопки призыва к действию.
  - `cta_link` — строка/URL, ссылка кнопки CTA.

2. Подключи эти поля к форме редактирования продукта `/admin/decisions/products/:id/edit`, не изменяя и не удаляя существующую структуру и бизнес-логику:
  - Не трогай блок выбора услуг, изображения, HTML-контент и порядок.
  - Поля должны сохраняться и читаться через текущую CMS-систему.
  - Все новые поля опциональные, без строгой валидации.

3. Убедись, что:
  - Контент сохраняется и отображается корректно.
  - Верстка в CMS админке не ломается.
  - Адаптивность и текущие компоненты интерфейса сохраняются.

Пример использования:
- `short_description` — будет использоваться в карточке и первом экране.
- `card_preview_image` — будет подгружаться в блоках на главной/каталоге.
- `page_title`, `page_subtitle`, `cta_text`, `cta_link` — переопределяют текстовую часть страницы продукта.

Важно: не менять существующий UI/UX, только добавить новые поля в редактор и модель.

 2 Задача: подключить ранее добавленные поля продукта в UI, без ломки текущей логики, только через мягкие fallback-механизмы.

1. **В карточках решений (листинги):**
  - Использовать `card_preview_image`, если оно задано. Если нет — использовать текущее изображение.
  - Использовать `short_description`, если заполнено. Если нет — ничего не менять.

2. **На странице продукта:**
  - Заголовок (`H1`): брать из `page_title`, если поле пустое — fallback на старое `title`.
  - Подзаголовок (`lead` / `description`): использовать `page_subtitle`, если оно заполнено.
  - CTA-кнопка:
    - Текст: использовать `cta_text`
    - Ссылка: использовать `cta_link`
    - Если любые из этих полей пустые — fallback на текущую реализацию/логику кнопки.

3. Никаких изменений:
  - В логике селектов услуг
  - В блоке HTML-контента
  - В бизнес-расчетах или параметрах продукта

Цель: сделать новые поля доступными в интерфейсе продукта **без поломки текущих механизмов**, с максимально безопасной интеграцией.

3 Задача: сделать контент главной страницы управляемым через CMS.

---

1. Найди существующую сущность для настроек главной страницы (например, `HomePageSettings` или `HomePageContent`).  
   - Если она уже есть — **расширь её**.  
   - Если отсутствует — **создай новую**.

---

2. Добавь следующие поля:

**HERO-блок:**
- `hero_title`
- `hero_subtitle`
- `hero_button_text`
- `hero_button_link`

**Блок "Подберите решение":**
- `select_title`
- `select_subtitle`

**Блок "Как мы работаем":**
- `work_title`
- `work_items[]` — массив объектов `{ title, text }`
- `work_button_text`
- `work_button_link`

**Блок "Частые вопросы":**
- `faq_title`
- `faq_items[]` — массив объектов `{ question, answer }`

**Блок "Почему выбирают нас":**
- `benefits_title`
- `benefits_items[]` — массив объектов `{ title, short_text, icon? }`

**Блок "Остались вопросы":**
- `contact_title`
- `contact_subtitle`
- `contact_form_hint_text`

---

3. В Admin UI:
- Настрой отображение этих полей с человекочитаемыми лейблами.
- Добавь подсказки к каждому полю: `"Отображается на главной странице"`.

---

Важно:
- Не менять и не затрагивать верстку компонентов.
- Только подключить данные из CMS в существующие шаблоны и компоненты.

4 Задача: заменить захардкоженные тексты главной страницы на данные из сущности `HomePageSettings`.

---

Требования:

1. Для каждого текстового блока:
  - Подключить соответствующее поле из `HomePageSettings`.
  - Если поле пустое → использовать текущий захардкоженный текст как fallback.

2. Не менять:
  - Структуру страницы
  - Layout и верстку
  - Порядок и наличие блоков

---

Работаем аккуратно:
- Только замена источников текста на данные из CMS
- Без изменения логики, компонентов, структуры или внешнего вида страницы

5 Задача: унифицировать типографику и вертикальные отступы секций на главной странице.

---

1. Заголовки секций:
- Привести заголовки следующих блоков к единому стилю:
  - "Подберите"
  - "Как мы работаем"
  - "Частые вопросы"
  - "Почему выбирают нас"
  - "Остались вопросы"

Требования:
- Все заголовки должны быть `H2`
- Один и тот же `font-size`, `font-weight`, `line-height`
- Базовый стиль берем с заголовка блока **"Остались вопросы"** — используем его как эталон

---

2. Вертикальные отступы:
- Привести `padding-top` и `padding-bottom` всех секций к одному значению
- Обеспечить единый вертикальный ритм по всей странице
- Значение отступов — ориентироваться на текущие отступы у блока "Как мы работаем" (если они корректные), либо задать разумные значения (например, 80–120px)

---

Ограничения:
- **Не менять** DOM-структуру, сетку, вложенность элементов
- Только корректировка классов или CSS/SCSS переменных
- Максимально чистая и предсказуемая типографика

6 Задача: привести все CTA-кнопки на сайте к единому стилю, используя существующий компонент.

---

1. Использовать текущий компонент кнопки, применяемый в форме футера (с закруглёнными краями) как **единственный источник стиля**.

2. Проверить и унифицировать следующие параметры на всех кнопках:
- Одинаковая высота
- Одинаковый `padding`
- Единый `border-radius`
- Поведение на `hover`, `active`, `focus` — соответствующее кнопке из футера

3. Все CTA-кнопки на сайте:
- Должны использовать **только этот компонент**
- Без локальных/дублирующих CSS-стилей или inline-правок

---

Важно:
- **Не создавать новый компонент или дизайн**
- Только переиспользовать и централизовать текущую реализацию кнопки

7 Задача: переработать футер сайта MNKA с сохранением фирменного стиля, шрифтов и цветовой палитры, используя структуру и наполнение, вдохновлённое примером с ru-center.

---

1. Общая структура футера:
Разделить контент на 4 колонки (или адаптивно 2 на мобилке), с использованием сетки текущего сайта.

**A. О компании:**
- О MNKA
- Команда
- Контакты
- Реквизиты

**B. Клиентам:**
- Решения
- Услуги
- Кейсы
- Вопросы и ответы (FAQ)

**C. Социальные сети:**
- VK
- Instagram
- Telegram  
(все иконки — SVG, одинаковый стиль, кликабельны)

**D. Документы и политика:**
- Политика конфиденциальности
- Обработка персональных данных
- Пользовательское соглашение

---

2. Нижняя строка футера:
- Копирайт: `© MNKA 2026. Все права защищены.`
- Платёжные/партнёрские логотипы (если применимо — в фирменной стилистике, монохром)
- Мини-ссылки: “Сообщить о нарушении”, “Правила применения”, и пр. — по желанию

---

3. Визуальные требования:
- Использовать текущую цветовую схему (зеленовато-серый фон, белый или светлый текст)
- Шрифт: тот же, что и в остальной части сайта (без замены типографики)
- Единая высота строк, аккуратные отступы (вертикальный ритм как в основной сетке)
- Иконки социальных сетей — того же размера и стиля, что в текущем футере
- Кнопки и ссылки — без underline, с чётким hover-состоянием

---

4. Технические требования:
- Реализовать через текущую компонентную систему (например, `<Footer />`)
- Поддержка адаптивности: мобильная версия сворачивается в 2 колонки, затем в 1
- **Не использовать захардкоженные значения** — если часть данных можно вынести в CMS (например, e-mail, копирайт, соцсети), подключить

---

Важно:
- Сохраняем визуальную преемственность и стиль сайта MNKA
- Не дублируем стили, не создаём новые шрифты
- Только аккуратная реструктуризация и усиление футера