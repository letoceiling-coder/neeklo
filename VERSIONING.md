# Версионирование проекта

## Текущая версия: v1.0.0

## 📦 Установка определенной версии

Проект использует семантическое версионирование (Semantic Versioning). Для установки конкретной версии используйте git теги.

### Как установить определенную версию

#### Вариант 1: Клонировать сразу нужную версию

Этот вариант наиболее быстрый и рекомендуется для новых установок. Клонируется только указанная версия без всей истории репозитория.

```bash
# Клонировать сразу версию 1.0.0
git clone --branch v1.0.0 --depth 1 https://github.com/letoceiling-coder/neeklo.git

# Для другой версии просто замените номер:
git clone --branch v1.1.0 --depth 1 https://github.com/letoceiling-coder/neeklo.git
git clone --branch v2.0.0 --depth 1 https://github.com/letoceiling-coder/neeklo.git
```

**Преимущества:**
- Быстрое клонирование (только нужная версия)
- Меньше места на диске
- Готово к использованию сразу после клонирования

#### Вариант 2: В существующем репозитории

Если у вас уже есть клон репозитория, переключитесь на нужную версию:

```bash
# Сначала получить все теги из удаленного репозитория
git fetch --tags

# Переключиться на нужную версию
git checkout v1.0.0

# Для другой версии:
git checkout v1.1.0
git checkout v2.0.0
```

**Если нужно вернуться на последнюю версию (main):**
```bash
git checkout main
git pull origin main
```

**Важно:** После переключения на конкретную версию, вы находитесь в состоянии "detached HEAD". Для работы рекомендуется создать новую ветку:
```bash
git checkout -b v1.0.0-work v1.0.0
```

### После установки версии

После установки конкретной версии выполните стандартную установку:

```bash
# Установить зависимости PHP
composer install

# Установить зависимости Node.js
npm install

# Скопировать файл окружения
cp .env.example .env

# Сгенерировать ключ приложения
php artisan key:generate

# Выполнить миграции
php artisan migrate

# Собрать фронтенд (если нужно)
npm run build:admin
npm run build:react
```

## 🔍 Просмотр доступных версий

Перед установкой конкретной версии полезно посмотреть список всех доступных версий:

```bash
# Список всех тегов (версий)
git tag -l

# Список тегов с сортировкой (от новых к старым)
git tag -l --sort=-v:refname

# Показать последние 10 версий
git tag -l --sort=-v:refname | head -10

# Информация о конкретной версии (коммит, автор, дата)
git show v1.0.0

# История изменений между версиями
git log v1.0.0..v1.1.0 --oneline
```

## 📋 История версий

### v1.0.0 (Январь 2026)

**Статус:** Стабильный релиз

**Основные изменения:**
- Первый стабильный релиз проекта Neeklo
- Полностью функциональная система на базе Laravel 11
- React фронтенд приложение
- Vue админ-панель
- Система авторизации и управления пользователями
- Медиа-библиотека с древовидной структурой
- Система поддержки с интеграцией внешних CRM
- Система уведомлений
- Интеграция с Telegram Mini Apps
- Автоматический деплой через GitHub Actions
- Полная документация проекта

**Технологический стек:**
- Backend: Laravel 11, PHP 8.2+
- Frontend: React 18 + TypeScript (пользовательская часть), Vue 3 + TypeScript (админ-панель)
- База данных: MySQL/PostgreSQL
- Деплой: GitHub Actions, автоматические webhooks

## 🔄 Обновление документации при изменении версий

При создании новой версии обязательно обновить:
1. Раздел "Текущая версия" в начале файла
2. Добавить новую версию в "История версий" с полным описанием изменений
3. Указать дату релиза, статус, основные изменения
4. Обновить README.md с актуальной информацией о версии

## 📝 Формат версий

Проект следует [Semantic Versioning](https://semver.org/):
- **MAJOR** - несовместимые изменения API
- **MINOR** - новая функциональность с обратной совместимостью
- **PATCH** - исправления ошибок с обратной совместимостью

Примеры:
- `v1.0.0` - первый стабильный релиз
- `v1.1.0` - новая функциональность
- `v1.1.1` - исправление ошибок
- `v2.0.0` - крупные изменения, возможна несовместимость

---
**Репозиторий:** https://github.com/letoceiling-coder/neeklo

