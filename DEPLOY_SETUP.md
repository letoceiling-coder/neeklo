# 📋 Пошаговая инструкция: Настройка деплоя на другом компьютере

## ⚠️ Проблема

Ошибка 403 возникает потому, что на новом компьютере сохранены **старые креденшалы GitHub** (Neeklo1606) или они **отсутствуют**. Нужно настроить правильную аутентификацию.

---

## 🖥️ Выберите вашу операционную систему

- [**macOS**](#-macos-инструкция) - команды для Mac
- [**Windows**](#-windows-инструкция) - команды для Windows

---

# 🍎 macOS Инструкция

## 🎯 Решение: Вариант 1 - SSH (РЕКОМЕНДУЕТСЯ)

SSH - самый безопасный и удобный способ. Один раз настроите и будет работать везде.

### Шаг 1: Проверить наличие SSH ключа

Откройте Terminal и выполните:

```bash
# Проверить наличие SSH ключа
if [ -f ~/.ssh/id_rsa.pub ] || [ -f ~/.ssh/id_ed25519.pub ]; then
    echo "SSH ключ найден!"
    cat ~/.ssh/id_rsa.pub 2>/dev/null || cat ~/.ssh/id_ed25519.pub
else
    echo "SSH ключ не найден. Создадим новый..."
fi
```

### Шаг 2: Создать SSH ключ (если его нет)

Если ключа нет, создайте его:

```bash
# Создать SSH ключ (замените email на свой)
ssh-keygen -t ed25519 -C "ваш-email@example.com"

# На вопрос "Enter file in which to save the key" нажмите Enter (используется путь по умолчанию)
# На вопрос "Enter passphrase" можете нажать Enter (без пароля) или задать пароль
```

### Шаг 3: Добавить SSH ключ в GitHub

1. **Скопируйте содержимое публичного ключа:**
```bash
# Для ed25519 (рекомендуется)
pbcopy < ~/.ssh/id_ed25519.pub

# Или для rsa
pbcopy < ~/.ssh/id_rsa.pub

# Или просто вывести на экран
cat ~/.ssh/id_ed25519.pub
```

2. **Откройте GitHub:**
   - Перейдите на https://github.com/settings/keys
   - Или: GitHub → Settings → SSH and GPG keys → New SSH key

3. **Добавьте ключ:**
   - Title: любое название (например, "MacBook Pro")
   - Key: вставьте скопированный ключ (Cmd+V)
   - Нажмите "Add SSH key"

### Шаг 4: Добавить SSH ключ в ssh-agent

```bash
# Запустить ssh-agent
eval "$(ssh-agent -s)"

# Добавить ключ в ssh-agent
ssh-add ~/.ssh/id_ed25519
# Или для rsa:
# ssh-add ~/.ssh/id_rsa
```

### Шаг 5: Проверить SSH подключение

```bash
ssh -T git@github.com
```

**Ожидаемый результат:**
```
Hi letoceiling-coder! You've successfully authenticated, but GitHub does not provide shell access.
```

⚠️ Сообщение "does not provide shell access" - это **нормально**, это не ошибка!

### Шаг 6: Удалить старые HTTPS креденшалы (если есть)

```bash
# Удалить креденшалы из Keychain Access
git credential-osxkeychain erase
host=github.com
protocol=https
# Нажмите Enter дважды

# Или через команду (удалить все GitHub креденшалы)
security delete-internet-password -s github.com 2>/dev/null
```

### Шаг 7: Переключить remote на SSH

Перейдите в папку проекта и выполните:

```bash
cd ~/path/to/neeklo  # Замените на путь к вашему проекту

# Проверить текущий remote
git remote -v

# Переключить на SSH
git remote set-url origin git@github.com:letoceiling-coder/neeklo.git

# Проверить, что переключилось
git remote -v
```

**Должно показать:**
```
origin  git@github.com:letoceiling-coder/neeklo.git (fetch)
origin  git@github.com:letoceiling-coder/neeklo.git (push)
```

### Шаг 8: Проверить деплой

```bash
php artisan deploy --dry-run
```

Если всё правильно, деплой должен работать без ошибок 403!

---

## 🔐 Решение: Вариант 2 - Personal Access Token (если SSH не подходит)

Если по каким-то причинам SSH не подходит, используйте Personal Access Token (PAT).

### Шаг 1: Создать Personal Access Token

1. **Откройте GitHub:**
   - Перейдите на https://github.com/settings/tokens
   - Или: GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)

2. **Создайте новый токен:**
   - Нажмите "Generate new token" → "Generate new token (classic)"
   - Note: любое название (например, "Деплой neeklo")
   - Expiration: выберите срок действия (например, 90 дней или No expiration)
   - Scopes: выберите **`repo`** (даёт полный доступ к репозиториям)

3. **Скопируйте токен:**
   - ⚠️ **ВАЖНО**: токен показывается только один раз! Сохраните его в безопасном месте.

### Шаг 2: Удалить старые креденшалы

```bash
# Удалить все старые креденшалы GitHub из Keychain
security delete-internet-password -s github.com 2>/dev/null

# Или через git credential helper
git credential-osxkeychain erase <<EOF
host=github.com
protocol=https
EOF
```

### Шаг 3: Настроить Git для использования токена

```bash
cd ~/path/to/neeklo  # Замените на путь к вашему проекту

# Убедиться, что remote использует HTTPS
git remote set-url origin https://github.com/letoceiling-coder/neeklo.git

# Проверить
git remote -v
```

### Шаг 4: При первом push Git запросит креденшалы

При выполнении `git push` или `php artisan deploy`:

1. **Username:** введите `letoceiling-coder`
2. **Password:** вставьте **токен** (не пароль от GitHub!)

Git сохранит эти креденшалы в macOS Keychain.

### Шаг 5: Проверить деплой

```bash
php artisan deploy --dry-run
```

---

## ✅ Быстрая проверка: Что настроено правильно? (macOS)

Выполните команды и проверьте результаты:

```bash
# 1. Проверить remote (должен быть SSH или HTTPS)
git remote -v

# 2. Проверить Git user (должен быть letoceiling-coder)
git config --global user.name
git config user.name

# 3. Для SSH: проверить подключение
ssh -T git@github.com

# 4. Проверить SSH ключи в ssh-agent
ssh-add -l

# 5. Для HTTPS: проверить сохраненные креденшалы в Keychain
security find-internet-password -s github.com 2>/dev/null
```

---

## 🔧 Дополнительная настройка Git (если не настроено) - macOS

Если Git не настроен глобально:

```bash
# Настроить имя пользователя
git config --global user.name "letoceiling-coder"

# Настроить email (замените на свой)
git config --global user.email "your-email@example.com"

# Настроить credential helper для macOS (если еще не настроен)
git config --global credential.helper osxkeychain

# Проверить настройки
git config --global --list
```

---

# 🪟 Windows Инструкция

## 🎯 Решение: Вариант 1 - SSH (РЕКОМЕНДУЕТСЯ)

SSH - самый безопасный и удобный способ. Один раз настроите и будет работать везде.

### Шаг 1: Проверить наличие SSH ключа

Откройте PowerShell или командную строку и выполните:

```powershell
# Проверить наличие SSH ключа
if (Test-Path "$env:USERPROFILE\.ssh\id_rsa.pub") { 
    Write-Host "SSH ключ найден!" 
    Get-Content "$env:USERPROFILE\.ssh\id_rsa.pub"
} else { 
    Write-Host "SSH ключ не найден. Создадим новый..." 
}
```

### Шаг 2: Создать SSH ключ (если его нет)

Если ключа нет, создайте его:

```powershell
# Создать SSH ключ (замените email на свой)
ssh-keygen -t ed25519 -C "ваш-email@example.com"

# На вопрос "Enter file in which to save the key" нажмите Enter (используется путь по умолчанию)
# На вопрос "Enter passphrase" можете нажать Enter (без пароля) или задать пароль
```

### Шаг 3: Добавить SSH ключ в GitHub

1. **Скопируйте содержимое публичного ключа:**
```powershell
Get-Content "$env:USERPROFILE\.ssh\id_rsa.pub" | Set-Clipboard
```

2. **Откройте GitHub:**
   - Перейдите на https://github.com/settings/keys
   - Или: GitHub → Settings → SSH and GPG keys → New SSH key

3. **Добавьте ключ:**
   - Title: любое название (например, "Рабочий компьютер")
   - Key: вставьте скопированный ключ (Ctrl+V)
   - Нажмите "Add SSH key"

### Шаг 4: Проверить SSH подключение

```powershell
ssh -T git@github.com
```

**Ожидаемый результат:**
```
Hi letoceiling-coder! You've successfully authenticated, but GitHub does not provide shell access.
```

⚠️ Сообщение "does not provide shell access" - это **нормально**, это не ошибка!

### Шаг 5: Удалить старые HTTPS креденшалы (если есть)

```powershell
# Проверить сохраненные креденшалы
cmdkey /list | findstr /i "git github"

# Удалить старые креденшалы (если найдены)
cmdkey /delete:"LegacyGeneric:target=git:https://github.com"
cmdkey /delete:"LegacyGeneric:target=git:https://letoceiling-coder@github.com"
```

### Шаг 6: Переключить remote на SSH

Перейдите в папку проекта и выполните:

```powershell
cd C:\OSPanel\domains\neeklo

# Проверить текущий remote
git remote -v

# Переключить на SSH
git remote set-url origin git@github.com:letoceiling-coder/neeklo.git

# Проверить, что переключилось
git remote -v
```

**Должно показать:**
```
origin  git@github.com:letoceiling-coder/neeklo.git (fetch)
origin  git@github.com:letoceiling-coder/neeklo.git (push)
```

### Шаг 7: Проверить деплой

```powershell
php artisan deploy --dry-run
```

Если всё правильно, деплой должен работать без ошибок 403!

---

## 🔐 Решение: Вариант 2 - Personal Access Token (если SSH не подходит)

Если по каким-то причинам SSH не подходит, используйте Personal Access Token (PAT).

### Шаг 1: Создать Personal Access Token

1. **Откройте GitHub:**
   - Перейдите на https://github.com/settings/tokens
   - Или: GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)

2. **Создайте новый токен:**
   - Нажмите "Generate new token" → "Generate new token (classic)"
   - Note: любое название (например, "Деплой neeklo")
   - Expiration: выберите срок действия (например, 90 дней или No expiration)
   - Scopes: выберите **`repo`** (даёт полный доступ к репозиториям)

3. **Скопируйте токен:**
   - ⚠️ **ВАЖНО**: токен показывается только один раз! Сохраните его в безопасном месте.

### Шаг 2: Удалить старые креденшалы

```powershell
# Удалить все старые креденшалы GitHub
cmdkey /delete:"LegacyGeneric:target=git:https://github.com"
cmdkey /delete:"LegacyGeneric:target=git:https://letoceiling-coder@github.com"
```

### Шаг 3: Настроить Git для использования токена

```powershell
cd C:\OSPanel\domains\neeklo

# Убедиться, что remote использует HTTPS
git remote set-url origin https://github.com/letoceiling-coder/neeklo.git

# Проверить
git remote -v
```

### Шаг 4: При первом push Git запросит креденшалы

При выполнении `git push` или `php artisan deploy`:

1. **Username:** введите `letoceiling-coder`
2. **Password:** вставьте **токен** (не пароль от GitHub!)

Git сохранит эти креденшалы в Windows Credential Manager.

### Шаг 5: Проверить деплой

```powershell
php artisan deploy --dry-run
```

---

## ✅ Быстрая проверка: Что настроено правильно? (Windows)

Выполните команды и проверьте результаты:

```powershell
# 1. Проверить remote (должен быть SSH или HTTPS)
git remote -v

# 2. Проверить Git user (должен быть letoceiling-coder)
git config --global user.name
git config user.name

# 3. Для SSH: проверить подключение
ssh -T git@github.com

# 4. Для HTTPS: проверить сохраненные креденшалы
cmdkey /list | findstr /i "git github"
```

---

## 🔧 Дополнительная настройка Git (если не настроено)

Если Git не настроен глобально:

```powershell
# Настроить имя пользователя
git config --global user.name "letoceiling-coder"

# Настроить email (замените на свой)
git config --global user.email "your-email@example.com"

# Проверить настройки
git config --global --list
```

---

## ❌ Частые проблемы и решения

### Проблема 1: "Permission denied (publickey)"

**Решение:** SSH ключ не добавлен в GitHub. Выполните Шаг 3 из варианта 1.

### Проблема 2: "fatal: unable to access 'https://github.com/...': The requested URL returned error: 403"

**Решение:** 
- Удалите старые креденшалы (см. Шаг 5 варианта 1 или Шаг 2 варианта 2)
- Убедитесь, что используете правильный токен или SSH ключ

### Проблема 3: Git запрашивает креденшалы каждый раз

**Решение:** 
- **Windows:** Для HTTPS: убедитесь, что креденшалы сохранены в Windows Credential Manager. Для SSH: проверьте, что SSH ключ добавлен в ssh-agent
- **macOS:** Для HTTPS: убедитесь, что креденшалы сохранены в Keychain (`git config --global credential.helper osxkeychain`). Для SSH: добавьте ключ в ssh-agent (`ssh-add ~/.ssh/id_ed25519`)

### Проблема 4: "This repository does not exist or you don't have access"

**Решение:** Убедитесь, что:
- Вы авторизованы как правильный пользователь (`letoceiling-coder`)
- У вас есть доступ к репозиторию
- URL репозитория правильный

---

## 📝 Чеклист настройки

### Для macOS:
- [ ] SSH ключ создан (или PAT создан)
- [ ] SSH ключ добавлен в GitHub (или токен сохранен безопасно)
- [ ] SSH ключ добавлен в ssh-agent (`ssh-add ~/.ssh/id_ed25519`)
- [ ] SSH подключение работает: `ssh -T git@github.com` (или креденшалы для HTTPS настроены)
- [ ] Старые креденшалы удалены из Keychain Access
- [ ] Remote переключен на SSH: `git@github.com:letoceiling-coder/neeklo.git` (или оставлен HTTPS)
- [ ] Git user настроен: `letoceiling-coder`
- [ ] Credential helper настроен: `git config --global credential.helper osxkeychain` (для HTTPS)
- [ ] Проверка деплоя: `php artisan deploy --dry-run` работает без ошибок 403

### Для Windows:
- [ ] SSH ключ создан (или PAT создан)
- [ ] SSH ключ добавлен в GitHub (или токен сохранен безопасно)
- [ ] SSH подключение работает: `ssh -T git@github.com` (или креденшалы для HTTPS настроены)
- [ ] Старые креденшалы удалены из Windows Credential Manager
- [ ] Remote переключен на SSH: `git@github.com:letoceiling-coder/neeklo.git` (или оставлен HTTPS)
- [ ] Git user настроен: `letoceiling-coder`
- [ ] Проверка деплоя: `php artisan deploy --dry-run` работает без ошибок 403

---

## 🎯 Рекомендация

**Используйте SSH (Вариант 1)** - это безопаснее и удобнее:
- Один раз настроили - работает везде
- Не нужно помнить токены
- Безопаснее (приватный ключ хранится локально)
- Не истекает (в отличие от токенов)

---

## 📞 Нужна помощь?

Если что-то не работает, проверьте:
1. Все шаги выполнены по порядку
2. SSH ключ/токен добавлен в правильный GitHub аккаунт (`letoceiling-coder`)
3. Старые креденшалы удалены
4. Remote URL правильный

